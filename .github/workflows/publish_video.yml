name: Publish Video Automated
on:
  push:
    branches: [main]
  workflow_dispatch: 

jobs:
  publish:
    runs-on: ubuntu-latest
    env:
      ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
      PEXELS_API_KEY: ${{ secrets.PEXELS_API_KEY }}
      YT_CLIENT_ID: ${{ secrets.YT_CLIENT_ID }}
      YT_CLIENT_SECRET: ${{ secrets.YT_CLIENT_SECRET }}
      YT_REFRESH_TOKEN: ${{ secrets.YT_REFRESH_TOKEN }}

    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: {python-version: "3.11"}
      - run: |
          pip install gTTS requests google-generativeai pexels-api moviepy edge-tts
          sudo apt-get update && sudo apt-get install -y ffmpeg
      
      - name: Run Scripts
        run: |
          python3 scripts/generate_script.py
          python3 scripts/generate_voice.py
          python3 scripts/fetch_clips.py
          python3 scripts/assemble_video.py
          python3 scripts/upload_youtube.py
